import { agent } from "./agent";
import { connector } from "./connector";
import { guide } from "./guide";
import { category } from "./category";
import { page } from "./page";

export const schemaTypes = [agent, connector, guide, category, page];
